(function () {
    'use strict';

    angular
        .module('resources.voting', [])
        .service('VotingService', VotingService);

    function VotingService() {
        var voting = this;
        
    }
})();